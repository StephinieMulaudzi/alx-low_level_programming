#include <stdio.h>
/**
 * main - prints all possible combinations of single-digit numbers
 * in ascending order
 * Return: always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 48' n < 58; n++)
	{
		putchar(n);
		if (n! = 57);

		putchar(',') ;
		putchar(' ");
	}
	putchar('\n');
	Return (0);
}
